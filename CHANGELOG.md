# 版本历史（CHANGELOG）

发布固件统一存放在 `firmware_releases/`，文件名格式：
`atc1441_alternate_flashimg_vX.Y_YYYY-MM-DD_<大小>B.bin`
每条发布记录 SHA256，用于烧录后读回校验比对。

### 仓库结构变更（2026-09-11，不含固件改动）

- 根目录 5 个脚本移入 `tools/`：`bmp2epd_framebuffer.py`、`epd_image_tool.py`、
  `gen_epd_image.py`、`gen_test_image.py`、`list_ports.py`。
  **下方历史记录里出现的裸文件名，现在都在 `tools/` 下。**
- 根目录两份文档移入 `docs/`：`nowa213_flash_research.md`、`nowa213_wiring_flash_summary.md`。
- 生成物不再入库：`previews/`、`test_black_*`、`epd_preview/` —— 由 `tools/` 下的脚本
  重新生成，规则见 `.gitignore`。本文件记录的 `firmware_releases/*.bin` 是**交付物**，仍然入库。
- 补上 `LICENSE`（MIT，继承 atc1441 与 pvvx 上游）。
- 修正全仓库的屏驱型号笔误 **SSD1675 → SSD1680**（研究笔记保留原文并加勘误块）。

---

## v2.0 — 2026-09-04

**功能**：时间↔图片每分钟自动切换；用户图存 Flash（断电/深睡不丢，重启自动恢复）。

**改动文件**（`atc1441_src/Firmware/src/`）：
- `epd.h` — 新增 FEATURE 说明块、`USER_IMG_FLASH_ADDR 0x79000`、外部声明
- `epd.c` — 新增 `user_image_check_flash / user_image_save / user_image_restore`
  （Flash 持久化，替代 v1 的 5KB RAM 缓冲，修复 SRAM 溢出变砖）
- `epd_ble_service.c` — opcode `0x01` 推送后调用 `user_image_save()`
- `app.c` — 启动 `user_image_check_flash()`；`main_loop` 每分钟在时间与图间切换

**发布文件**：`firmware_releases/atc1441_alternate_flashimg_v2.0_2026-09-04_90972B.bin`
**SHA256**：`f81eba73651b941697d7d2d3750f2f11c7eb21fe15733dc67256511516a0e585`
**大小**：90972 字节

**烧录**：`python TLSR825xComFlasher.py -p COM6 -t 3000 wf 0 <上述文件>`

---

## 上游基线 — atc1441/ATC_TLSR_Paper（Stellar-M3N @ E31HA）

刷机用的原始 atc1441 固件：`TlsrComSwireWriter/atc1441_StellarM3N_E31HA.bin`（90840 字节）。
作为「官方 stock」救砖底牌，也用于回退验证 GPIO 兼容性。

> ⚠️ 汉朔原厂固件备份 `nowa213_original_backup.bin` 已损坏为 0 字节（丢失），
> 无法用于恢复原厂界面；如需原厂请从另一台同型号价签重新备份。

---

## v3.0 — 2026-09-04

**功能**：修复 BLE 上传图片的两个显示问题（相对 v2.0）。
- 水平翻转（镜像）：BW213 控制器默认列扫描为右→左，用户图未抵消该镜像，
  显示成左右镜像。新增 `user_image_flip_horizontal()`，在推送/保存前对图像做
  水平镜像，与时间页（FixBuffer 已镜像一次）行为一致。
- 右侧黑边：v2.0 推送/恢复时按 `epd_buffer_size`（5000 字节）发送，而实际屏
  面积为 250×128/8 = 4000 字节，多发的 1000 字节被渲染为右侧黑块。改为按
  新增宏 `EPD_DISPLAY_SIZE`（4000）发送与保存。

**改动文件**（`atc1441_src/Firmware/src/`）：
- `epd.h` — 新增 `EPD_DISPLAY_WIDTH/HEIGHT/SIZE` 宏；声明 `user_image_flip_horizontal()`
- `epd.c` — 新增 `user_image_flip_horizontal()`；`user_image_save/restore` 改用 `EPD_DISPLAY_SIZE`
- `epd_ble_service.c` — opcode `0x01` 推送前调用 `user_image_flip_horizontal()`，
  显示尺寸改为 `EPD_DISPLAY_SIZE`
- `app.c` — 每分钟恢复显示用户图时尺寸改为 `EPD_DISPLAY_SIZE`

**发布文件**：`firmware_releases/atc1441_alternate_flashimg_v3.0_2026-09-04_91052B.bin`
**SHA256**：`e84de57fb619c3edad5b8f7469c600846fc28e577bf70721bcd32622a40993f0`
**大小**：91052 字节

**烧录**：`python TLSR825xComFlasher.py -p COM6 -t 3000 wf 0 <上述文件>`

> ⚠️ 升级提示：v3 改变了 Flash 中用户图的存储尺寸（v2 存 4096 字节，v3 存
> 4000 字节，布局仍从 `0x79000+4` 起、magic 不变）。旧 v2 存的用户图在 v3 下
> 仍可被 `user_image_restore` 读回并显示，但方向仍是镜像的（v2 存的是未镜像
> 原图，v3 恢复时不再翻转），因此**建议重传一次图片**使镜像方向正确。

---

## 开发工具（随 v3.0 之后加入）

**`nowa213/epd_image_tool.py`**：加速图片方向验证，无需反复刷机+蓝牙上传。
- **PC 预览**：输入图片/原始帧缓冲，秒出两张 PNG：
  - `*_v3_fixed.png` — 预测 v3 固件刷入后屏幕显示的正确方向。
  - `*_v2_buggy.png`  — 预测 v2 固件时的镜像方向。
- **直写 Flash（跳过蓝牙）**：`--bake` 生成 `*_flash_sector.bin`（4096 字节，
  含 `IMG1` magic + 已镜像的 4000 像素），然后用 SWS 刷机命令只写
  `0x79000` 扇区即可：
  ```
  python TLSR825xComFlasher.py -p COM6 -t 3000 wf 0x79000 <name>_flash_sector.bin
  ```
  断电重启后价签即显示新图。v3 固件只须刷一次，之后换图不必再走 BLE。

---

## 开发工具更新 — 250×122 有效区域修正（2026-09-04）

**问题**：测试图在屏幕上看不到下边框，底部横条与屏幕底边之间还有一段白条。
**根因**：面板存储缓冲区是 250×128（16 字节/列、整字节对齐），但实际玻璃只露出约
**250×122** 行，底部 6 行画到了玻璃外面。之前 `epd_image_tool.py` 与测试图按 250×128
作画，所以底边被裁掉。

**修正**：
- `epd_image_tool.py`：新增 `VISIBLE_H = 122`，输入图先缩放到 **250×122** 可见区域，
  再顶部对齐填充到 250×128 存储缓冲区（底部 6 行留白）。预览图也裁到 122 行，
  与屏幕上实际看到的一致。
- `gen_test_image.py`：改为按 250×122 有效区域绘制边框、方向箭头和底部标记。
- `epd.h`：新增 `EPD_VISIBLE_HEIGHT 122` 宏并补充注释，明确物理可见高度。

**验证**：重新生成测试图 → `epd_image_tool.py --bake --flash` 直写 `0x79000` → 断电重启
后应能看到四边完整黑框。

> 提示：以后制作图片只需关注 250×122 区域；底部 6 行不会显示在玻璃上，工具会自动
> 帮你补白对齐。

---

## 网页传图不同步时间 — 根因与修复工具（2026-09-04）

**现象**：用网页工具传图后，价签时间没有同步。

**根因（已读 atc1441 传图工具源码确认）**：时间同步与传图是**两条不同的 BLE 特征**：
- 传图：`EPD_BLE` 服务 `13187b10-…` / 特征 `4b646063-…`，协议 `0000`→`020000`→`03`分块→`01`。
- 时间同步：`RxTx` 服务 `0x1f10` / 特征 `0x1f1f`，命令 `0xDD` + 4 字节大端 Unix 时间戳
  （固件 `cmd_parser.c` 处理，已在 RxTx 特征上正确接线）。
- atc1441 的传图工具 `connect()` **只连 EPD_BLE 特征，全程不发 `0xDD`、甚至不连 RxTx 特征**。
  所以“传图时同步时间”不会发生——命令根本没发。这**不是固件 bug**（固件 `0xDD` 处理正确）。

**修复**：新增 `web_uploader.html`（WebBLE 单页工具，无需改固件、无需重刷）：
- 载入图片 → 按固件期望的 `col*16 + row//8`（MSB=顶，黑=0）打包为 4000 字节；
- 复用 atc1441 已验证的传图协议发到 EPD_BLE 特征；
- 随后向 RxTx 特征写 `0xDD` + 本地 Unix 时间（默认 `UTC+8`，唐山/北京时区）完成校时；
- 含实时预览（直接渲染 `buf`，即屏幕最终样子，已抵消固件翻转+面板扫描）；
- “上传图片” / “同步时间” / “上传+同步时间” 三个按钮，可只用校时按钮配合原工具。
- 用法：`cd nowa213 && python -m http.server 8000`，浏览器开
  `http://localhost:8000/web_uploader.html`（WebBLE 需 localhost/https，勿用 file://）。

**说明**：`0xDD` 设时间后屏幕不会立即刷新（固件只在每分钟交替或下次传图时重绘时间页），
最迟 1 分钟内显示新时间；想立刻看到可再点一次“上传图片”。

### 修复 — WebBLE 连接报错 `Invalid Service name: '1f10'`（2026-09-10）

**问题**：`requestDevice` 抛 `TypeError: Invalid Service name: '1f10'`。

**根因**：Web Bluetooth 不接受 16 位短 UUID 别名（`'1f10'` / `'1f1f'`）。
固件 GATT 表中 RxTx 服务/特征确实是 16 位 UUID（`my_RxTx_ServiceUUID = 0x1f10`、
`my_RxTxUUID = 0x1f1f`），但浏览器要求写成完整 128 位形式。

**修复**（`web_uploader.html`）：
- `RXTX_SERVICE` → `00001f10-0000-1000-8000-00805f9b34fb`
- `RXTX_CHAR`    → `00001f1f-0000-1000-8000-00805f9b34fb`
- 顺手修掉“重连”按钮的真 bug：原实现 `device.gatt.connect().then(connect)` 会再次调用
  `requestDevice` 弹出设备选择框；现拆分为 `setupServices()` + `reconnect()`，重连同一设备、不再弹窗。

**顺带核实（无需改动）**：
- ATT 写入不做长度校验：`my_EPD_BLE_Data` / `my_RxTx_Data` 均为单字节 `u8`，而传图每包写
  240 字节一直正常，证明 Telink 栈按 `rf_packet_att_data_t.dat[]` 透传完整负载，
  因此 5 字节的 `0xDD + 时间戳` 不会被截断；`cmd_parser.c` 正是从 `req->dat[1..4]` 组装时间。
- 时区不会被二次叠加：固件 `app.c` 直接用 `get_time()` 做 `%24` / `%60` 取时分，不施加任何
  偏移，故工具发送 `Date.now()/1000 + 8*3600`（东八区本地时间）是正确的。

---

## v4.0 — 2026-09-10（纯时钟模式）

**需求**：不再需要「时间 ↔ 图片」每分钟交替，价签一直显示时间。

**改动**（`atc1441_src/Firmware/src/app.c`）：
- `main_loop()` 的整分 tick 里删除交替分支，直接 `epd_display(get_time(), ...)`，
  即每分钟只重绘时间/状态页。
- `user_init_normal()` 中注释掉 `user_image_check_flash()`（不再需要启动时读 flash 标志）。
- 保留 `epd.c` 中的 `user_image_*` 与 `epd_ble_service.c` 的 0x01 路径不变：
  BLE 上传的图片仍会**立即显示一次**，下一分钟整分刷新时自动回到时钟。
- 代码内留下注释说明如何恢复交替功能（重新调用 `user_image_check_flash()` +
  在整分分支里按 `has_user_image && display_toggle` 分流）。

**构建**：`_end_bss_ = 0x84efc1`（栈顶 0x850000，余量 4159 B，与 v3.0 相同，无 SRAM 风险）。

**发布文件**：`firmware_releases/atc1441_clockonly_v4.0_2026-09-10_90988B.bin`
**SHA256**：`dfafb06bb668c54b4b88d63b7819dcb08d1f24cf2ac7daf33d298a3f3493b2aa`
**大小**：90988 字节

**烧录**：`python TLSR825xComFlasher.py -p COM6 -t 3000 wf 0 <上述文件>`

**另修（`web_uploader.html`）**：校时日志时间显示错误。`t` 已是「UTC+8 的 epoch」，
原代码再用 `toLocaleString()` 按浏览器本地时区渲染 → 二次加 8 小时（显示 22:09 而实际 14:09）。
改为用 `getUTC*` 拼字符串输出，并标注 `(UTC+8)`。**写入设备的时间戳本身一直是对的**，
固件 `time.c` 存原始值、`app.c` 用 `%24`/`%60` 取值，不施加任何时区偏移。

---

## 工具新增 — `web_flasher.html` 网页刷固件（BLE OTA，2026-09-10）

**背景**：CH340 不在时无法用 SWS 串口刷机，改走设备自带的 BLE OTA 通道。

**协议事实（对照 `Firmware/src/ota.c` + 上游 ATC_TLSR_Paper 逐一核实）**：
- OTA 是 atc1441 **自定义实现**，不是 Telink SDK OTA。服务/特征 `0x221f` / `0x331f`
  （`my_OtaServiceUUID` / `my_OtaUUID`），属性 `NOTIFY | WRITE`。
- 镜像放到 OTA bank `0x20000`（`OTA_BANK_START`，大小 `0x20000` = 128 KB），
  命令流：`01` 擦 4 KB 扇区 → `03` 填充 256 字节 RAM 缓冲 → `02` 落一个 bank →
  `07 C001CEED <crc>` 触发 `write_ota_firmware_to_flash()`：擦 `0x00000–0x1FFFF`、
  整块拷贝到地址 0、重启。
- **`0x79000` 用户图扇区与 `0x78000` 设置扇区不在擦除范围内，不受影响。**
- 固件头 bytes[8..11] 必须是 `4B 4E 4C 54`（"TLNK"）；本 `ATC_Paper.bin` 通过。

**发现并规避的坑（已用离线仿真证明）**：
`case 7` 的「CRC 校验」比较的是**设备自己的** `out_buffer[5..6]` 与 `crc_out`，
而不是主机发来的 CRC 字节。正常流程下最后一条 `02` 把两者都清成 0，所以该判断等价于
「只认 magic 词」，上传的 CRC 被完全忽略 —— **OTA 实际上没有完整性校验**。
更进一步：一旦先发过 `04`（回读）或 `06`，`out_buffer` 被污染，magic 门禁反而会失败。
因此本工具在提交前**多发一条 `05 00000000`**，把 `out_buffer` 显式归零，
使门禁确定性通过，从而让「先回读校验、再提交」这一更安全的流程成立。

**`web_flasher.html` 相比官方工具的安全性改进**：
1. 载入 `.bin` 时校验 TLNK 固件头与大小上限，拒绝非法文件；
2. 上传完成后**默认逐字节回读整个镜像比对**（cmd `04`，约 4550 次读），
   全部一致才提交；任何不一致直接中止且**不提交** → 设备继续跑旧固件，不会变砖；
3. 提交前用 cmd `05` 归零门禁；
4. 内置「同步时间」按钮（`0xDD`）——OTA 后设备时间归零，必须重新校时。

**配套离线验证**：`tools/ota_protocol_sim.js`（Node，无依赖）用真实 `ATC_Paper.bin`
重放完整流程，忠实模拟 `ota.c` 状态机与 `write_ota_firmware_to_flash()`，断言：
擦除彻底、回读一致、门禁通过、终态 `0x00000` 处镜像逐字节等于 `.bin`、
镜像之后为 `0xFF`、`0x40000+` 未被触碰；并验证「先回读不归零则拒绝提交」、
「单字节损坏会被回读抓到且不提交」。全部通过。

**用法**：`python -m http.server 8000` → `http://localhost:8000/web_flasher.html`
→ 连接 → 选 `firmware_releases/atc1441_clockonly_v4.0_2026-09-10_90988B.bin` → 开始刷写
→ 刷完设备自动重启断连 → 重新「连接」→ 点「同步时间」。

---

## v5.0 — 2026-09-10（省电优化：A + C + D）

**背景**：e-paper 静止不耗电，耗电只发生在刷新那一刻。当前固件已经在用「每分钟一次
轻量 LUT（10 帧）+ 每小时一次全刷（OTP 波形）」的方案，本版进一步削减三处开销。

> 说明：原计划一并实现「真·局部窗口刷新」（方案 B），经代码核查后**推迟到 v6.0 单独做**。
> 原因：本机 `epd_bw_213.c`（model 1）全程未设置 `0x11` 数据入口模式与 `0x44/0x45`
> RAM 地址窗口，走的是控制器默认值；而局部窗口依赖这些寄存器的坐标语义。另一个驱动
> `epd_bwr_213.c` 里能看到 `0x44 -> 0x00,0x0F`（X 为 **字节单位**，16 字节 = 128 像素），
> 可作参考，但必须先做一次坐标标定，不能与已验证的省电改动混刷（出问题分不清是谁的锅）。

**改动**：

1. **A. 轻量刷新帧数 10 → 7**（`epd_bw_213.c`，`lut_bw_213_refresh_time`）
   每分钟那次刷新的波形帧数减少 30%，面板驱动能耗随之下降（估算 20~30%）。
   代价：残影略增，由每小时一次的全刷清除。若字迹发灰/发虚，把常量改回 8 或 10 即可。

2. **C. 夜间静默刷新**（`app.c`，`NIGHT_START_HOUR 0` / `NIGHT_END_HOUR 6`）
   `0:00–5:59` 完全不驱动面板（内部时钟照常走）。因为 `hour_refresh` 仍在更新，
   早上 6:00 的第一次重绘自动是 **全刷**，顺便清掉夜间积累的残影。
   加 `first_refresh_done` 保证上电后**必定先画一次**——否则刚开机（时间=0，落在夜间
   区间）会一直白屏。想关掉夜间静默就把两个宏都设为 0。

3. **D. BLE 广播间隔 1 s → 10 s**（`app_config.h`，`ADVERTISING_INTERVAL 1600 → 16000`，
   单位 0.625 ms）。广播是持续开销，拉长间隔直接省电；BLE 规范上限 10.24 s（16384），
   16000 合法。代价：手机/浏览器搜到设备最多要多等 1 个间隔（约 10 s），OTA 后重连时
   会更明显。

4. **附带修复**：BLE 校时（`0xDD`）现在**立即重绘**，不再等下一个整分。
   `time.c` 新增 `RAM uint8_t time_just_set`（`set_time()` 置位），`app.c` 的
   `main_loop()` 消费后置零。这同时解决了「夜间静默期间校时看不到变化」的体验问题。

**保持不变（重要）**：`e-paper` 的静态画面依旧零耗电；刷新完成后 `epd_set_sleep()` 立刻
`EPD_POWER_OFF()` 让面板深睡。LED 每 10 秒只亮 1 ms（≈0.005 mAh/天），可忽略；
电池/温度读取在首次之后走缓存，也不再真读面板。

**构建**：`_end_bss_ = 0x84efc1`（栈顶 0x850000，余量 4159 B，与 v3.0/v4.0 相同，无 SRAM 风险）。

**发布文件**：`firmware_releases/atc1441_clockonly_v5.0_2026-09-10_90988B.bin`
**SHA256**：`b7026d780001e641a7d04a94a80c6dff8429dc7c2eec94204d251bd7f2ecff52`
**大小**：90988 字节

**烧录**：`python -m http.server 8000` → `http://localhost:8000/web_flasher.html`
→ 选本文件 → 开始刷写（保持回读校验）→ 重启后重新连接 → **同步时间**。

---

## v6.0 — 2026-09-10（真·局部窗口刷新，省电方案 B）

**目标**：把每分钟那次刷新从「扫描整屏」变成「只驱动时间数字所在矩形」，进一步降低面板刷新能耗。

### 关键前提修正（对着 IL0373 官方手册逐寄存器核实）

先纠正一个此前文档里的错误认知：这块屏的控制器是 **IL0373**（命令集 `0x00 PSR / 0x61 TRES / 0x50 CDI / 0x20-0x24 LUT / 0x10 DTM1 / 0x13 DTM2 / 0x12 DRF / 0x07 DSLP` 与手册完全一致），
与 `epd_bwr_213.c` 用的 **SSD1675/SSD1680 是不同家族**。由此得：

| 事实 | 说明 |
|---|---|
| **IL0373 没有 `0x44/0x45/0x4E/0x4F` RAM 窗口寄存器** | 唯一的窗口机制是 `0x90 PTL` + `0x91 PTIN` / `0x92 PTOUT` |
| PTL 固定 7 字节 | `HRST[7:3]`、`HRED[7:3]`、`VRST[8]`、`VRST[7:0]`、`VRED[8]`、`VRED[7:0]`、`PT_SCAN` |
| H 轴＝**源极**＝122 像素可见轴 | 以 **8 像素 bank** 寻址，写入值 = `像素 & 0xF8` |
| V 轴＝**栅极**＝250 像素轴 | 9 位像素值，高字节在前；`glass_x = 249 - gate` |
| `PT_SCAN = 0` | **"只扫描窗口内的 gate"**（默认 1＝只扫窗口外）。这才是真正省电的那一位 |

> ⚠️ 特别注意：**IL0373 的 `0x11` 是 Data Stop，不是"数据入口模式"**。Waveshare 2.13" V3 驱动里的 `0x11=0x03` 属于另一种控制器，照抄会写坏寄存器。

### 数据通路刻意不动

仍按原样发送完整 250×16 字节整帧，**只缩小被扫描的窗口**。因此即使窗口算错，最坏结果也**不是花屏**，而是"时间数字不再更新"——把 `EPD_USE_PARTIAL_WINDOW` 改成 0 即可一键回退。

### 窗口数值来源（不是估的）

用 `DSEG14_Classic_Mini_Regular_40` 的真实字形度量 + `obdWriteStringCustom()` 的实际排版算法（`obd.inl`，y 为基线），穷举全部 1440 个时刻得到墨迹范围：

- 数字墨迹：玻璃 `x ∈ [54,190]`，`y ∈ [25,64]`
- 实际窗口（两侧留余量）：玻璃 `x ∈ [50,194]`，`y ∈ [24,71]`
- 生成寄存器字节：`HRST=0x18`(bank 3)、`HRED=0x40`(bank 8)、`VRST=55`、`VRED=199`、`PT_SCAN=0`

### 改动

- `epd_bw_213.c`：新增 `EPD_BW_213_set_partial_window()`（`0x91`→`0x90`→7 字节），在 `full_or_partial == 0` 路径中于数据写入前调用；`full_or_partial != 0` 路径**先发 `0x92` PTOUT**，防止全刷继承 PTL 窗口导致残影清不干净。窗口常量集中在文件顶部，含 `#error` 静态断言。
- `app.c`：新增 `last_ble_shown / last_temp_shown / last_batt_shown` 跟踪。窗口外的字段（`ESL_` 标题、BLE 指示、温度、电量）一旦变化就强制整屏全刷，否则它们会一直停在旧值。电量用 **32 mV 迟滞**，否则 ADC 的 1 mV 抖动会让窗口几乎每分钟失效。
- `tools/verify_partial_window.py`：离线几何校验器，从字体度量穷举重算墨迹范围，断言其被窗口包含，并打印应写入的 PTL 字节。

### 验证

- 编译通过；`_end_bss_ = 0x84efc1`（栈顶 `0x850000`，余量 4159 B，与 v3/v4/v5 相同，无 SRAM 风险）。
- **反汇编交叉验证**：`tc32-elf-objdump` 显示实际指令序列为
  `0x91 → 0x90 → 0x18 → 0x40 → 0x00 → 0x37 → 0x00 → 0xC7 → 0x00 → 0x10`，
  与离线脚本预测的 PTL 字节**逐字节一致**。
- `verify_partial_window.py` 全部断言 PASS。

### 预期收益（诚实估计）

| 项 | 数值 |
|---|---|
| 扫描 gate 数 | 145 / 296 → **减少 51%**（相对面板实际 250 条 gate 则为 145/250，减少 42%） |
| 驱动源极数 | 48 / 128 → 减少 62% |
| 每帧源×栅驱动量 | **约 1/5.4（减少约 82%）** |

> 这是按数据手册模型推算的**理论值**，未做电流实测。真实节电比例还受 LUT 帧数、波形电平影响。

**发布文件**：`firmware_releases/atc1441_clockonly_v6.0_2026-09-10_90988B.bin`
**SHA256**：`e4c96ba85b80790e99fc41c9019b738eab5f3d1adb26563da3a97447976549f6`
**大小**：90988 字节

**烧录**：`python -m http.server 8000` → `http://localhost:8000/web_flasher.html`
→ 选本文件 → 开始刷写（保持回读校验）→ 重启后重新连接 → **同步时间**。

### 上机后如何判定成功/失败

- ✅ **成功**：秒针不显示，但**分钟数字每分钟正常跳变**，屏幕其余内容不变。
- ❌ **窗口算错**：屏幕看起来一切正常，但**分钟数字不再变化（时间冻住）**。
  → 把 `epd_bw_213.c` 的 `EPD_USE_PARTIAL_WINDOW` 改为 `0` 重新编译即可回退到 v5.0 行为。

---

## v7.0 — 2026-09-11（真·局部窗口刷新，搬到了正确的驱动上）

### 前提纠正：v5.0 / v6.0 的省电改动落在了不会执行的文件里

价签屏幕左上角由 `epd.c:355` 打印 `epd_model_string[epd_model]`，实机显示
**`ESL_xxxxxx BWR213`** → `epd_model == 2`。而 `epd.c:193-204` 的分发是
model 1 → `EPD_BW_213_Display()`（`epd_bw_213.c`）、model 2 → `EPD_BWR_213_Display()`
（`epd_bwr_213.c`），且 `set_EPD_model()` 只在 `cmd_parser.c:66` 被 BLE 命令调用、启动时不设。

结论：**本机实际跑的是 `epd_bwr_213.c`**；`epd_bw_213.c`（UC8151/IL0373 族）只是
detect 全失败时的兜底分支（`epd.c:134`）。因此：

- v5.0 的 LUT 帧数 10→7（写在 `epd_bw_213.c`）**从未执行**；
- v6.0 的 IL0373 `0x90 PTL` 局部窗口 + `0x92 PTOUT`（写在 `epd_bw_213.c`）**从未执行**，
  v6.0 实际是 no-op。

v7.0 把真·局部窗口做进 `epd_bwr_213.c`。v6.0 的代码保留不动（对 BW213 面板仍是对的），
v5.0 的 LUT 改动仍待移植。

### 手册依据：SSD1680 没有 PTL，但有「只驱动部分栅极」的两把钥匙

SSD1680 手册 Rev 0.14（`SSD1680_DIE01129S.pdf`）：

| 寄存器 | 位置 | 语义 |
|---|---|---|
| `0x01` Driver Output Control | p.34 | `MUX[8:0]` = 驱动线数 − 1；范围 16~296 MUX。byte1=MUX[7:0]、byte2=MUX[8]、byte3=GD,SM,TB |
| `0x0F` Gate Scan Start Position | p.36 | `SCN[8:0]` = 起始栅极，取值 0~295 |

p.36 Figure 8-2 把语义演示得很清楚：MUX ratio = 093h、Gate Start Position = 04Ah 时，
**G0~G73 不被驱动（表中记 `-`）**，G74 起才驱动，且 G74 = ROW74、G75 = ROW75（恒等映射，
正是本驱动 GD=0 / SM=0 所选的映射）。

所以 `MUX[8:0] = N−1` 配 `SCN = F` 就精确驱动连续区间 `G_F..G_(F+N−1)`，
区间外的栅极不被驱动 → **保留上一次全刷的墨迹**。这正是我们要的：只重写时钟数字，
其余画面原地不动。刷新耗时正比于扫描栅极数，这就是省电来源。

### 几何推导（不是猜的）

```
FixBuffer() : epd_buffer[col*16 + byteY] = ~ucMirror[ obd[byteY][249-col] ]
RAM 写入    : 下标 i = col*16 + byteY  ->  RAM X = byteY, RAM Y = 296 - col
              (0x11 = 0x01：X 递增、Y 递减；0x4F = 0x0128)
因此        : glass_x = 249 - col = RAM_Y - 47   ->   RAM_Y = glass_x + 47
```

时钟墨迹在全部 1440 个时刻覆盖 glass x [54,190]（由 `font_60.h` 真实字形度量穷举得到）
→ RAM 行（栅极号）**[101,237]**，共 137 条。

### 改动（`atc1441_src/Firmware/src/epd_bwr_213.c`）

新增 `EPD_USE_GATE_WINDOW` 开关 + 窗口常量 + `#error` 静态断言，并在
`EPD_BWR_213_Display()` 的 `0x01` 写入处按 `full_or_partial` 分支：

- 局部刷新：`0x01` 写 `MUX[8:0]=136`（137 线）、byte3 仍为 `0x01`；随后 `0x0F` 写 `SCN=101`
- 全刷：保持原样 `0x28 0x01 0x01`（296 线）

**数据通路刻意完全不动**：仍然把完整的 250×16 字节整帧写到同样的 RAM 地址，
`0x44/0x45/0x4E/0x4F` 一个都没碰。只有「驱动哪些栅极」变了，所以窗口算错也不会撕裂画面，
最坏只是数字不更新；把 `EPD_USE_GATE_WINDOW` 改成 `0` 即回到旧行为。

### 验证

- `_end_bss_ = 0x84efc1`，与 v3.0~v6.0 完全一致（余量 4159 B）→ 无 SRAM 风险
- `tools/verify_gate_window.py` 全部断言 PASS，输出：
  - 时钟墨迹 glass x [54,190] → RAM 行 [101,237]
  - `0x01` 应发 `88 00 01`、`0x0F` 应发 `65 00`
- 反汇编核对（`tc32-elf-objdump -d out/epd_bwr_213.o`），局部路径实际发出：
  `WriteCmd(0x01) → WriteData(0x88) → WriteData(0x00) → WriteData(0x01) →
   WriteCmd(0x0F) → WriteData(0x65) → WriteData(0x00)` —— 与脚本预言**逐字节一致**
- 全刷路径仍为 `28 01 01`（反汇编 0x92~0x9e 处确认）

### 预期收益（手册模型，未做电流实测）

扫描栅极 **137/296 = 46%**，即刷新耗时与驱动功耗约为原来的一半。数据量不变
（仍整帧写 RAM），所以省的是面板驱动部分，不是 SPI。

### 上机后如何判定成功/失败

- ✅ **成功**：分钟数字照常每分钟跳变，且屏幕其余部分（标题、温度、电量行）保持正常。
- ❌ **失败 A（数字冻住）**：说明被驱动的栅极区间没覆盖到数字 → 把窗口放宽（例如
  `EPD_WIN_GATE_FIRST 90` / `EPD_WIN_GATE_LAST 250`）重编译再试。
- ❌ **失败 B（数字错位/花屏）**：说明 `0x0F` 起始栅极或 MUX 语义理解有偏差 →
  把 `epd_bwr_213.c` 的 `EPD_USE_GATE_WINDOW` 改为 `0` 重新编译，即回到 v6.0 行为。

**发布文件**：`firmware_releases/atc1441_clockonly_v7.0_2026-09-11_90988B.bin`
**SHA256**：`c5fce54b513dc01e6f17caaf7675802afbf7a75bafe91adc3ce6d634da2a769a`
**大小**：90988 字节

---

## v8.0 — 2026-09-11（局部刷新波形帧数 50 → 40）

### 前提：先把手册语义钉死（SSD1680 Rev 0.14）

`LUT_bwr_213_part[]` 是写到寄存器 `0x32` 的 **153 字节波形设置**。布局（Figure 6-6, p.15，
由新增的 `tools/verify_part_lut.py` 从源码解析并逐项断言）：

| WS 字节 | 内容 |
|---|---|
| 0..59   | VS[nX-LUTm]，5 个 LUT × 12 字节（12 组 × 4 相 × 2 bit） |
| 60..119 | TP[nX] + RP[n]，12 组 × (TP_A, TP_B, TP_C, TP_D, RP) |
| 120..143| SR[nAB], SR[nCD]，12 组 × 2 |
| 144..149| FR[n]，12 组 × 3 bit（补成半字节） |
| 150..152| XON[nAB/CD]，12 组 × 2 bit |
| 153..158| 属寄存器 0x3F / 0x03 / 0x04 / 0x2C，**不在 0x32 载荷内** |

解析结果：**整张表里 TP/RP/SR 只有 WS[60] = TP[0A] 非零**（其余 TP=0 → 该相跳过，
RP/SR=0 → 重复 1 次），因此局部刷新的波形长度**恰好等于 WS[60]** —— 也就是源码里的
`#define BWR_213_Len 50`。五个型号（BWR213 / BWR154 / BW213ice / BWR350 / BWY350）
上游一律写 50，从未改过。

顺带纠正一个容易误解的地方：每帧里 `0x22 = 0xB1` + `0x20` 那一步，在手册的 0x22 选项表里是
"Enable clock / **Load temperature value** / **Load LUT with DISPLAY Mode 2** / Disable clock"
—— 它**只锁存温度、把 OTP LUT 载入寄存器，不做显示驱动**。真正的驱动是后面
`0x22 = 0xC7`（"Display with DISPLAY Mode 2"）那一次。所以每分钟只有一次面板驱动，
没有隐藏的整屏刷新。

### 改动

`atc1441_src/Firmware/src/epd_bwr_213.c`：`BWR_213_Len` 50 → 40（−20%）。仅此一行。

- **影响面只有每分钟的时钟刷新**。图片上传走 `epd_ble_service.c:43` 的
  `EPD_Display(epd_buffer, EPD_DISPLAY_SIZE, 1)`（全刷路径），用的是 OTP 波形，不受影响。
- 代价：波形变弱 → 数字对比度略降 / 可能留残影；但同一像素每分钟会被再驱动一次，
  且**每小时整点的全刷（OTP 波形、296 栅极）会把对比度重新建立**。
- 回退：改回 50 即回到 v7.0 行为；若 40 看着完全结实，可以再试 32。
- ⚠️ 低温需要更长的脉冲，所以别把价签放到冷环境（冰柜 / 无暖气房间）后再调低这个值。

### 验证

- 新增 `tools/verify_part_lut.py`：解析源码 LUT → 断言 153 字节、布局正确、只有第 0 组
  相位 A 非零、FR 半字节全为 2、XON 全 0 → 全部 PASS；并能在固件镜像里定位这张表
  （v7.0 命中 WS[60]=50，v8.0 命中 WS[60]=40）。
- `_end_bss_ = 0x84efc1` —— 与 v3.0~v7.0 完全一致（余量 4159 B）。
- **逐字节对比 v7.0 与 v8.0**：90988 字节中只有 5 个字节不同 ——
  `0x16098: 0x32(50) → 0x28(40)`（.data 载入镜像里的 TP[0A]）＋末尾 4 字节 CRC。
  这是最有力的证据：改动确实是外科手术式的。
- 附带查清：镜像里第二处相同的 153 字节表是 `LUT_bwr_154_part`（1.54" 型号，波形恰好相同），
  不是本数组的陈旧副本 —— 用 `nm -S` 的符号地址可对上：`.data` LMA 0x15FB0 + 0xA8 = 0x162AC。

### 预期收益

每分钟面板驱动时间 ≈ 40/50 = **80%**（这是在 v7.0 已把驱动栅极从 296 降到 137 之后）。
理论值，未做电流实测。

### 上机判定

- ✅ 成功：分钟数字照常跳变，黑度/对比度与 v7.0 无可见差别。
- ⚠️ 数字发灰，或整点全刷后一段时间内出现残影 → 把 `BWR_213_Len` 改回 50 重编译。
- 判据要等**至少 10 分钟**（跨一次十位分钟跳变）和**一次整点全刷**之后再看。

**发布文件**：`firmware_releases/atc1441_clockonly_v8.0_2026-09-11_90988B.bin`
**SHA256**：`4840335ec4eefce8b5fe25357dbc089d4718238fc395c7dbba74935b733e6c86`
**大小**：90988 字节

---

## v9.0 — 2026-09-11（时钟走得慢：每调用只加 1 秒）

### 现象

价签上的"一分钟"对应现实 2 分钟甚至更久，无法精确统计。

### 根因：`handler_time()` 用 `if` 而不是 `while`，把时钟速率卡在了"主循环调用频率"上

`app.c` 的 `main_loop()` 第一句就是 `blt_sdk_main_loop()`，而 BLE 协议栈就在这里面**睡到下一个事件**；
真正的睡眠入口是 `blt_brx_sleep()` → `cpu_sleep_wakeup()`：

- 反汇编链接后的镜像：`blt_sdk_main_loop` 位于 `0x39b0`，紧邻的 `blt_brx_sleep` 位于 `0x3f28..0x4386`，
  而 `blt_brx_sleep` **函数体内的字面量池**（`0x4270`）里就是 `&cpu_sleep_wakeup`（`0x0084efb4`）。
  → 外层循环每转一圈就睡一次，`handler_time()` 因此**远低于每秒一次**。
- v5.0 把 `ADVERTISING_INTERVAL` 从 1600（1 s）改成 16000（10 s），唤醒频率再降 10 倍，
  于是每次调用之间的间隔达到数秒量级。

而旧代码是：

```c
if (clock_time() - last_clock_increase >= one_second_trimmed)
{
    last_clock_increase += one_second_trimmed;
    current_unix_time++;          // 每次调用最多 +1 秒
}
```

它隐式假设"主循环每秒被调用很多次"。这个假设不成立之后，时钟速率就被钳制在
**（每秒调用次数 × 1 秒）**上，而不是由真实时间驱动 → 走得慢，且慢多少取决于睡眠节奏。
这也解释了为什么这个问题在 v5.0 之后才暴露出来。

### 为什么 `clock_time()` 可以直接用来追补

`clock_time()` 读的是 16 MHz 系统定时器（`timer.h`：「system Timer : 16Mhz, Constant」，
寄存器 `reg_system_tick` = `0x00800740`，与 CPU 主频设置无关），而且**协议栈会在每次唤醒时把它补回来**：

- `main.c` 调用 `blc_pm_select_internal_32k_crystal()`，其中 `pm_tim_recover = pm_tim_recover_32k_rc`；
- 反汇编该函数（库 `liblt_8258.a`，`pm_32k_rc.o`）得到：

```
sys_tick = tick_cur + ((now_32k - last_32k) * tick_32k_calib) >> 4
```

即用 32k RTC（休眠期间仍在跑）线性外推系统定时器。所以跨休眠的 `clock_time()` 是连续的，
可以直接当作真实时间使用。注意 `PM_LONG_SLEEP_WAKEUP_EN` 为 0、`pm_get_32k_tick` 在内部 32k RC
模式下并未被赋值，所以不应改用它——真正的实时基准就是被补回来的系统定时器。

### 改动

`atc1441_src/Firmware/src/time.c`：`if` → `while`（一行关键字 + 注释）。仅此一处。

```c
while (clock_time() - last_clock_increase >= one_second_trimmed)
{
    last_clock_increase += one_second_trimmed;
    current_unix_time++;
}
```

每轮循环只有几条指令；即使一口气积压一小时（3600 轮）也只是微秒级。

### 验证

- 新增 `tools/verify_time_catchup.py`：对同一套唤醒时序分别模拟新旧函数体，全部断言 PASS。
  按 10 s 广播间隔、每次唤醒跑 6 轮主循环建模：
  **旧代码计数速率 0.594（1.7 倍慢），新代码 0.999**；
  且新代码在 adv ∈ {0.05, 1, 10, 60, 200} s、burst ∈ {1, 2, 3, 4, 6, 200} 的组合下
  滞后量**恒定为约 +2 秒**（1 秒"已完成整秒"的固有滞后 + `time_trime` 312 ppm），不随唤醒节奏增长。
- **反汇编并排对比**（`objdump -d out/time.o`）：
  - v8.0：读完一次 `clock_time()` 后直接 `tjex lr` 返回 —— 只可能 +1 秒；
  - v9.0：`0x30` 重新读 `clock_time()`，`0x38` 处 `tjcs.n 28` 回跳成环。
- **段大小对比**（用 `git worktree` 从 tag `v8.0` 独立重建基线）：
  `.ram_code` 0x4b70 → 0x4b80（+16 B，就是那个循环）；
  `.text` / `.data` / `.bss` **大小完全不变**；`.retention_data` 仅整体上移 0x10
  （所以镜像里其余差异都是指向 `RAM` 变量的字面量地址 +0x10）。
  `_end_bss_ = 0x84efc1` —— 与 v3.0~v8.0 完全一致，无 SRAM 风险。
- 附带：从 tag `v8.0` 重建的 bin 与归档的 v8.0 固件**逐字节一致** → 构建可复现。

### 连带影响（都是修正，不是回归）

- `time_reached_period(Timer_CH_1, 30)`（电量/温度采样）此前实际每 ~5 分钟才触发一次，
  现在按设计每 30 秒触发一次；
- `time_reached_period(Timer_CH_0, 10)`（LED 指示）此前每 ~100 秒，现在每 10 秒一次
  （单次点亮 1 ms，功耗可忽略）。

### 已知边界

`clock_time()` 是 32 位、16 MHz，**268.4 秒回绕一次**。单次睡眠超过 268 秒时无符号差值会算错，
追补失效。当前 `ADVERTISING_INTERVAL = 16000`（10 秒），实际不可能睡这么久；
但**如果以后关掉广播或把广播间隔调到分钟级，这个修复就会失效**。
（`tools/verify_time_catchup.py` 里用 `adv=300` 显式复现并断言了这个边界。）

### 上机判定

- ✅ **成功**：价签时间与手机/电脑时间同步走，几分钟内看不出偏差。建议放置 10~30 分钟后再看。
- ❌ **仍然偏慢（例如走 1 分钟用 1.5 分钟）**：说明系统定时器并未被真正补回，
  那就要改用 32k tick 作为时基，需要另做诊断固件（通过 RxTx 通道上报
  `current_unix_time` / `clock_time()` / `cpu_get_32k_tick()` 三个计数器来定标）。
- ⚠️ **轻微偏慢（每天 ~27 秒）**：这是 `time_trime = 5000`（+312 ppm）造成的，属另一个问题。
  先确认粗差已消除，再把 `time_trime` 调到 0 实测一天；这一步按"单步微调"的原则单独做。

**发布文件**：`firmware_releases/atc1441_clockonly_v9.0_2026-09-11_90988B.bin`
**SHA256**：`9428b77dcf6effa693ee5d0e37633131bd93dc4de68ebf14be13f1bf57d0edfd`
**大小**：90988 字节

---

## v10.0 — 2026-09-11（屏幕右下角显示固件版本）

### 需求

在屏幕右下角显示当前固件版本，格式 `v<major>.<minor>`，字号与电压值相同。

### 实现（源码只新增 21 行，零删除）

| 文件 | 改动 |
|---|---|
| `app_config.h` | 新增 `#define FW_VERSION_STRING "v10.0"` |
| `epd.c` | 新增 `EPD_VERSION_X 199` / `EPD_VERSION_Y 120`，并在 `epd_display()` 里加两行 |

绘制刻意**沿用电压行的字体与基线**（`Dialog_plain_16`，基线 y=120），只把 x 改成右对齐：

```
x = 250 - 2 - width("v10.0") = 250 - 2 - 49 = 199
width 由 font16.h 的真实 xAdvance 相加：v=10, 1=11, 0=11, .=6  ->  49
```

`y` 是**基线**不是顶边（`obdWriteStringCustom()` 的语义），所以墨迹实际落在 y 108..119，
在可见玻璃（0..121）之内；x 墨迹 199..246，距右边缘 3 px。

### 与 v7.0 局部刷新窗口的关系：零成本

徽章墨迹在玻璃 x 199..246，由 `RAM_Y = glass_x + 47` 得栅极 **246..293**，
而每分钟驱动的窗口是栅极 101..237 → **徽章完全在窗口之外，不增加每分钟的刷新开销**。
版本号是编译期常量，只需要全刷；而全刷本来就会重画它（首次上电、整点、以及 BLE/温度/电量
变化触发的 force_full）。首次刷新一定是全刷（`hour_refresh` 初值 100 ≠ 任何合法小时），
所以一上电右下角就有版本号，不需要等整点。

### 验证

- **新增 `tools/verify_version_badge.py`（离线几何校验，全部 PASS）**
  从 `font16.h` / `font30.h` 解析**真实字形度量**，复现 `obdWriteStringCustom()` 的排版，
  并读取 `FW_VERSION_STRING` / `EPD_VERSION_X` / `EPD_VERSION_Y`：
  - `width("v10.0") = 49` 与 `EPD_VERSION_X = 199` 一致（**这条是防漂移断言**：
    以后把版本号改长了却忘了改 x，脚本会直接 FAIL）；
  - 墨迹 x [199,246] / y [108,119] 均在 250×122 可见区内；
  - 与最宽可能的电压行 `"Battery 65535mV"`（墨迹右边界 162）相距 **36 px**，不重叠；
  - 与温度行（y 73..95）在 y 方向不重叠；
  - 在每分钟窗口（gate 101..237）之外 → 零额外成本。
- **反汇编核对**（`tc32-elf-objdump -d out/epd.o`）：`epd_display` 里电压行之后确实新增
  `cmd → sprintf(buff,"%s",FW_VERSION_STRING)` → `obdWriteStringCustom(buff, Dialog_plain_16,
  x, y)`，且立即数就是 `tmovs r2, #199` / `tmovs r3, #120`，与脚本预言**逐字节一致**。
- **镜像里能找到该字符串**：`"v10.0\0"` 位于 0x113A4（前后紧邻 `"Battery %dmV"` 与
  `epd_model_string[0]="NC"` 的字符串池）。
- **段大小对比**（用 `git worktree` 从 tag `v9.0` 独立重建基线）：
  `.ram_code` 0x4b80 → 0x4ba0（**+32 B**，新代码）；
  `.rodata` 0x5750 → 0x5758（**+8 B**，就是 `"v10.0\0"`）；
  `.vectors` / `.text` / `.data` / `.bss` **大小完全不变**；
  `.retention_data` 仅 VMA 上移 0x20（所以镜像里其余差异都是指向 RAM 变量的字面量地址 +0x20，
  属预期涟漪）。`_end_bss_ = 0x84efc1` 与 v3.0~v9.0 **完全一致** → 无 SRAM 风险。
- **构建可复现**：从 tag `v9.0` 重建的 bin 与归档的 v9.0 固件逐字节一致。
- **新增 `tools/render_screen_preview.py`（离线屏幕预览器）**
  解析真实字体位图，把 `epd_display()` 的整屏内容渲染成 PNG（默认 3× 放大），
  另有右下角 6× 特写。以后任何排版改动都能先看效果再刷机，不用为了确认几个像素往返刷写。
  生成 `previews/screen_v10_zoom3.png`、`previews/badge_zoom6.png`、`previews/screen_v9_zoom3.png`。

### ⚠️ 发版流程：版本号要改三处

以后每次发版，除了 CHANGELOG / 归档 / 打 tag，**还要改 `app_config.h` 的
`FW_VERSION_STRING`**，否则屏幕上的版本号会滞后。`tools/verify_version_badge.py`
会检查字符串与 x 坐标是否匹配，但不会替你检查它是否跟上了 tag。

### 上机判定

- ✅ 右下角出现 `v10.0`，字体与 `Battery ...mV` 完全一致，位置不挤不越界。
- 若想微调位置：只改 `epd.c` 的 `EPD_VERSION_X`，然后跑一次
  `tools/verify_version_badge.py` 确认右对齐断言仍 PASS。

**发布文件**：`firmware_releases/atc1441_clockonly_v10.0_2026-09-11_90996B.bin`
**SHA256**：`26e57a6b644440218a9ff9e5b4654423cf0414816630741aa1c8cf074249d446`
**大小**：90996 字节

---

## v11.0 — 2026-09-11（修复「每隔几分钟整屏黑一次」；右上角 `B` → 蓝牙图标）

### 需求

1. 屏幕**每隔几分钟就整屏黑一次**（像刚刷完固件那样），而且间隔**不固定**（观察到 5 分钟、2 分钟）。
   问这是不是全屏刷新、为什么会这样。
2. 右上角连接后出现的字母 `B` 换成**蓝牙图标**。

### 现象确认

**是的，那就是全屏刷新。** 全刷走 `0x01` MUX=296 线的完整黑→白→黑波形，整屏可见；
v7.0 起的局部刷只驱动栅极 101..237，只有时钟那一段会闪。

而「**间隔不固定**」这一点很关键：它排除了任何**定时器**机制（代码里也确实没有 5 分钟周期，
`app.c` 只按"分钟数变化"和"小时数变化"刷新）。不固定 ⇒ 一定是某个**变化检测**在随机地
把 `force_full` 拉高。三个来源里 BLE 是事件驱动（不抖），于是只剩温度和电池。

### 根因：1 °C 量化的温度读数 + 每次刷新都会覆盖它

链条上的每一步都在代码里：

```
app.c:121        int16_t temp_now = EPD_read_temp();
app.c:122        if (temp_now != last_temp_shown) -> force_full = 1   // 任何 1 °C 变化都全刷

epd.c:150        EPD_read_temp(): if (epd_temperature_is_read) return epd_temperature;  // 返回缓存
epd.c:216        EPD_Display():   epd_temperature_is_read = 1;
epd.c:206        EPD_Display():   epd_temperature = EPD_BWR_213_Display(...);          // 每次刷新都覆盖
epd_bwr_213.c:310                 0x1B 读温度，位置在 0x20 Master Activation + CheckStatus 之后
```

也就是说：温度**不是在空闲时读的**，而是每次刷新过程中、boosters 正在给芯片加热时读的，
结果存进 `epd_temperature`，而下一次比较拿到的正是它。每次比较实际是
「第 N-1 次刷新末尾的芯片温度」对「第 N 次刷新末尾的芯片温度」。

SSD1680 内部传感器是 **1 °C 量化**的。只要环境温度落在某两个量化步之间，
这两次读数就会**在相邻两个值之间来回跳 1 度** —— 表现为"每 2 分钟全刷一次"
（正是后来补报的「这次两分钟又刷新一次」），跳得慢些就是 5 分钟一次。
**与任何定时器无关，只取决于环境温度恰好落在哪两个量化步之间**，所以间隔看起来是随机的。

电池同理：`get_battery_mv()` 每 30 s 采样，原来的 32 mV 迟滞不足以吸收 ADC 噪声。

### 实现

| 文件 | 改动 |
|---|---|
| `app.c` | 新增 `TEMP_FULL_HYSTERESIS_C 3` / `BATT_FULL_HYSTERESIS_MV 100`；温度比较由 `!=` 改为**死区**，电池 32 → 100 mV |
| `epd.c` | 新增 `BLE_ICON_BITS[13]` + `epd_draw_ble_icon()`，`EPD_USE_BLE_ICON` 一键回退；字母 `B` 的绘制改为条件编译 |
| `app_config.h` | `FW_VERSION_STRING` → `"v11.0"` |

改写后的温度比较（首次仍会触发，因为 `last_temp_shown` 初值 0x7FFF）：

```c
int16_t temp_now = EPD_read_temp();
if ((temp_now > last_temp_shown ? temp_now - last_temp_shown
                                : last_temp_shown - temp_now) >= TEMP_FULL_HYSTERESIS_C)
{
    last_temp_shown = temp_now;
    force_full = 1;
}
```

**明确说代价**：温度显示最多滞后 `阈值-1 = 2 °C`，直到下一次整点全刷才跳到最新值。
对一个一天才动几度的量，这不可见。想更灵敏就调小 `TEMP_FULL_HYSTERESIS_C`。

**另一个可选方案（本次未采用）**：把温度也纳进每分钟的局部窗口（栅极 101..237 扩到约 57..237）。
那样温度是实时的，但每分钟驱动线数从 137 涨到约 181（46% → 61%），省电效果变差。
选死区，是因为它对省电更友好、改动也最小。

### 蓝牙图标

原来是 `BLE_conn_string[] = {"", "B"}` 用 `Dialog_plain_16` 画在 `(232, 20)`。换成 7×13 位图：

```
...#...      # 竖茎在 x=3
...##..      斜线 (3,0)→(6,3)
...#.#.
...#..#
...#.#.      斜线 (6,3)→(3,6)
...##..
...#...
（下半部分同构： (3,6)→(6,9)→(3,12)）
```

`BLE_ICON_BITS[13]` 按「位 n → 像素 (x+n)」编码：
`08 18 28 48 28 18 08 | 18 28 48 28 18 08`。

**位置不是猜的**，是从它替换掉的那个字形反推的：`Dialog_plain_16` 的 `B` 度量是
`{w=10, h=12, xo=1, yo=-12}`，基线 20 ⇒ 墨迹 x 233..242（中心 237.5）、y 8..19（中心 13.5）。
7×13 的图标取 `x=234`（中心 237，差 0.5 px）、`y=8`（底边正好坐在原来那条基线上）。

绘制走 `obdSetPixel()`，和字体是同一条路径，因此直接落进 `epd_temp` 这个 `FixBuffer()`
会处理的缓冲区，不需要维护第二套像素格式。

### 验证

- **新增 `tools/verify_ble_icon.py`（13 项断言全 PASS）**：位图行数/位宽、竖茎贯穿全 13 行、
  四条 45° 斜线无断点、在 250×128 屏内、「型号行止于 x=189 < 图标起点 x=234」，
  以及**与字母 `B` 的垂直跨度、水平中心对齐**（防漂移断言）。
- **图标确实进了镜像**：`nm` 显示 `BLE_ICON_BITS` 位于 `0x00010d80`（`r` 段），
  并且能在 bin 偏移 **68992** 处搜到那 13 字节序列。
- **`tools/render_screen_preview.py` 升级**：不再硬编码 `(232,20)` 和字母 `B`，
  改为从 `epd.c` 解析图标几何与位图，预览与固件不可能各说各话。
  产出 `previews/screen_v11_zoom3.png`、`previews/ble_ind_zoom8.png`。
- **SRAM**：`_end_bss_ = 0x84efb1`，距栈顶 `0x850000` 还有 4175 B。
- 镜像 90996 → **91260 B**（+264 B：13 B 位图 + 绘制函数 + 两处死区比较）。

### 上机判定

- ✅ **期望**：整屏黑闪从「几分钟一次」变成**只在整点出现**（外加 BLE 连接/断开时）。
  夜间 0:00–5:59 仍然完全不刷。
- 若仍然频繁全闪 ⇒ 说明还有第四个触发源。**已由 v12.0 兑现**：给三个 `force_full`
  出口各挂一个计数器并直接显示在屏幕上，一次定位（见下）。
- ✅ 右上角在**连接时**显示蓝牙小图标；未连接时什么都不显示（与原来一致）。

**发布文件**：`firmware_releases/atc1441_clockonly_v11.0_2026-09-11_91260B.bin`
**SHA256**：`e286c9557ff1155ecb7af9d432ac67eabde8e9517b03027ea6d992389ddf3d5e`
**大小**：91260 字节

---

## v12.0 — 2026-09-11（全刷取证：让屏幕自己说清是哪一个原因）

**背景**：v11.0 给温度和电池加了死区后，用户复测**仍然全屏黑闪**，且间隔**不固定**
（11:35 / 11:37 / 11:41 / 11:42 / 11:46 ⇒ 2 / 4 / 1 / 4 分钟）。不定间隔排除了一切
定时器 —— `app.c` 里根本没有 N 分钟的周期，`full` 只能由四条路径拉起。与其继续推演
物理量（温度是刷新瞬间读寄存器、电池是单次采样无中值滤波，两者都说得通），不如把
四条路径各挂一个计数器**画到屏幕上**，一次上机定位。

**改动**（纯诊断；刷新策略与 v11.0 **完全一致**，不改变任何行为）：

| 文件 | 内容 |
|---|---|
| `app.c` | 新增 `dbg_hour/dbg_temp/dbg_batt/dbg_ble` + `dbg_armed`；三条 `force_full = 1` 各自配一个 `cause_* = 1`；只在**真正画出去**的全刷上 `DBG_BUMP`（饱和到 9） |
| `epd.c` | `EPD_USE_REFRESH_DEBUG 1` + `EPD_DEBUG_X 84` / `EPD_DEBUG_Y 95`，在温度行右侧画 `Hn Tn Bn Ln` |
| `epd.h` | 四个计数器的 `extern` |
| `app_config.h` | `FW_VERSION_STRING "v12.0"` |
| `tools/verify_refresh_debug.py` | 新增，30 项断言 |
| `tools/render_screen_preview.py` | 同步渲染诊断串（位置从 `epd.c` 解析，不重复硬编码） |

**读法** —— 温度右边那串 `H0 T0 B0 L0`：

| 字母 | 含义 | 正常值 |
|---|---|---|
| `H` | 小时变了 | 每小时 +1（设计内的整点全刷） |
| `T` | 面板温度越过死区 `TEMP_FULL_HYSTERESIS_C` | 应该**一直是 0** |
| `B` | 电池电压越过死区 `BATT_FULL_HYSTERESIS_MV` | 应该**一直是 0** |
| `L` | 蓝牙连接状态翻转 | 未连接时应为 0 |

数字饱和到 9。**若闪了多次而 H/T/B/L 全是 0 ⇒ 不在这四条路径上**，那就要往"设备在
复位"方向查（RAM 计数器会随复位一起清零，这本身就是个信号）。

> **v13.0 更新**：这一版的计数器上限有缺陷 —— `H` 记的是整点刷新，一天 18 次，
> 上限 9 必然撞顶，读数只能说"≥9"。格式串与上限都已在 v13.0 修正（见文末），
> 且 v13.0 出厂时计数器**已从玻璃上摘除**。

**哨兵抑制**：`dbg_armed` 让上电后第一次刷新不计数 —— 那一次
`last_ble_shown = 0xFF` / `last_temp_shown = 0x7FFF` 的比较不是真原因，否则一上电就会
显示 `H1 T1 B0 L1` 误导。

**几何**（`tools/verify_refresh_debug.py` 全部从源码解析后断言）：
- 温度 `24'C` 墨迹 x 10..71；诊断串从 x=84 起，最宽 `H9 T9 B9 L9` 墨迹 **x 85..189**
- 整串落在每分钟栅极窗口（玻璃 x 54..190）**内** ⇒ 每个 tick 都重画，永不发霉
- 与下方电池 / 版本号行（墨迹顶 y=107）留 12 px 间隙

**反汇编核对**（第三层验证）：`main_loop` 以 `r4 = 0x844e10` 为基址，用
`tloadrb/tstorerb [r4, #14..#18]` 依次读写 `dbg_armed / dbg_ble / dbg_temp / dbg_batt /
dbg_hour`，偏移与 `nm` 给出的 `0x844e1e..0x844e22` 完全吻合 ⇒ 递增逻辑确实编入、
未被优化掉。（**按全地址 grep 会漏检** —— TC32 用基址+偏移寻址，不是每个变量都进字面量池。）

**发布文件**：`firmware_releases/atc1441_clockonly_v12.0_2026-09-11_91276B.bin`
**SHA256**：`0e43d7b1ebd36d8652f0e5bd574e98f3279f35a62b7f282ea46e406884a19dc6`
**大小**：91276 字节
**SRAM**：`_end_bss_ = 0x84efb1`（与 v11.0 相同，距栈顶 0x850000 仍有 4175 B）

---

## v13.0 — 2026-09-12（结案：整屏黑闪消失；把计数器从玻璃上摘掉）

**结论**：v12.0 的计数器运行近一天后读数 `H9(饱和) T0 B1 L2`，用户目视观察期间
**未再出现整屏黑白黑闪烁** —— v11.0 的温度死区就是真正的修复，问题闭环。

### 读数解读

| 计数 | 读数 | 判读 |
|---|---|---|
| `H` 小时变化 | 9（饱和） | 撞上限，只能说明「≥9」（见下方"设计失误"） |
| `T` 温度越界 | **0** | 一整天温度从未达到 3 ℃ 死区 ⇒ **温度再没触发过全刷** |
| `B` 电池越界 | 1 | 电池正常放电掉过 100 mV，无害 |
| `L` BLE 翻转 | 2 | 连一次 + 断一次，正常 |

一天全刷 ≈ 18 次整点（6:00–23:00）+ 1 + 2 ≈ **21 次**；而 9 月 11 日报告的那组间隔
（2/4/1/4 分钟，均值 2.75 分钟）推算一天约 **500 次**。**降了 20 倍以上。**

**顺带更正一处此前的误判**：9 月 11 日那组闪烁时间（11:35/11:37/11:41/11:42/11:46）
**早于带温度死区的 v11.0（当天 11:58 才交付）**，来源是更早的版本。当时据此判断
"死区没解决问题"是错的 —— 那组数据恰恰是死区生效**之前**的症状。

### 改动

- `epd.c`：`EPD_USE_REFRESH_DEBUG 1` → **`0`**，计数器下屏，界面恢复干净。
  `app.c` 里的计数逻辑**故意保留** ⇒ 重新武装只需改这一行（每次全刷多几条指令，
  且不可能改变"何时驱动屏幕"）。
- `epd.c`：格式串 `"H%d T%d B%d L%d"` → **`"H%02dT%dB%dL%d"`**（去掉空格）。
- `app.c`：`DBG_BUMP` 拆为 `DBG_BUMP_H`（上限 **99**）与 `DBG_BUMP`（上限 9）。

**为什么要去空格**：`H` 记整点刷新，一天 18 次，一位数上限必然撞顶；改成两位数后
`"H99 T99 B99 L99"` 是 150 px、`"H99 T9 B9 L9"` 是 117 px（墨迹止于 **x=201**），
两者都**越出每分钟栅极窗口（玻璃 x 54..190）**，尾部将失去每分钟重画而发霉。
去空格后 `"H99T9B9L9"` 只有 99 px、墨迹 **x 85..182**，重新落回窗口内。

**这是 v12.0 的设计失误**：上限应当按"该事件在观察窗口内的预期次数"来定，
而不是按"版面能放几位"倒推。`T`/`B`/`L` 预期接近 0，一位数是对的；只有 `H` 需要两位。

### 验证

- `tools/verify_refresh_debug.py` 重写：**40 项断言全 PASS**。上限不再硬编码，改为从
  `app.c` 的宏解析；最宽串由 `max(上限位数, 格式字段宽度)` 算出，不再假设。
  新增**针对上述失误的回归断言** —— `dbg_hour` 上限必须 ≥ 一个白天的整点数
  （`24 − 夜间小时数` = 18），调回 9 会立刻 FAIL。
  （已离线验证该断言非空：把上限改回 9 确实被拦下。）
- **开关真的生效（不是假设）**：v13.0 镜像里**搜不到** `"H%02dT%dB%dL%d"`，
  而 `v13.0`、`"Battery %dmV"` 仍在 ⇒ `#if EPD_USE_REFRESH_DEBUG` 确实把绘制编译掉了。
- **反汇编核对（第三层）**：`main_loop` 以 `r4 = 0x844e10` 为基址，
  `dbg_ble / dbg_temp / dbg_batt` 的比较是 `tcmp r3, #8`（即 `< 9`），
  而 `dbg_hour` 是 **`tcmp r3, #98`（即 `< 99`）** —— 新上限确实编入镜像。
- 全部 **7 个断言脚本 PASS**；`_end_bss_ = 0x84efb1` 未变（距栈顶 0x850000 仍有 4175 B）。
- `tools/render_screen_preview.py` 新增 `--with-debug`：开关关闭时也能离线检查
  计数器排版；样本串改为从固件的格式串解析，不再有第二份硬编码副本。

**行为变化**：相对 v11.0 只有界面 —— 计数器下屏。刷新策略一个字节都没改。

**发布文件**：`firmware_releases/atc1441_clockonly_v13.0_2026-09-12_91260B.bin`
**SHA256**：`05b83be4024fddac6bf3c82e91ffd03b451b82db8d804a8b0b3d8dcce947cf30`
**大小**：91260 字节
**SRAM**：`_end_bss_ = 0x84efb1`

**如果整屏黑闪又回来**：把 `epd.c` 的 `EPD_USE_REFRESH_DEBUG` 改回 `1` 重新编译即可，
计数逻辑一直在，且这次 `H` 不再饱和、读数有定量价值。

---

## v14.0 — 2026-09-12（版面重做：公历 + 农历 + 节气 + 手绘大时钟；局部窗口 137 → 92 栅极）

**需求**：把屏面按参考照片改版 —— 第一行「日期 + 星期 + 温度 + 电压」、第二行大号时间、
第三行「农历 + 节气倒计时」、右下角蓝牙符文 + 设备名。可行性推导见
[docs/v14-reference-design.md](docs/v14-reference-design.md)（该文 §7 逐条对账了预测与实际）。

### 版面

```
行1  2026年9月12日 周六 31℃ 2905mV        ROW1_Y=2   Unifont 16（汉字 16 px / ASCII 8 px）
行2           19:06                       手绘 7 段，h68 w40 厚8 斜4 距8，槽位 [18,68,118,140,190]
行3  八月初二 11天后秋分        ⚡[A1B2C3]   ROW3_Y=104 符文槽 173..180，名字右对齐到 248
```

**行为变化（相对 v13.0）**：

- **去掉**屏顶 `ESL_xxxxxx BWR213` 型号行 —— 型号可经 BLE 读，而设备名现在直接印在行 3；
- **去掉** `Battery 2905mV` 那一行，电压并入行 1；`Battery` 字样不再出现在镜像里；
- ⚠ **去掉 v10.0 的右下角版本号**：新的行 3 右下角改画设备**广播名**（照参考面板的
  `[881A]`），版面没有多余空间。版本号从此只存在于源码、git tag 与发布文件名中。
  如需屏上可见，最省事的做法是让行 3 右端在「名字」与「版本」之间轮换。
- 时钟从 `DSEG14_Classic_Mini_Regular_40` 改为**手绘 7 段**：该字体宽高比 0.85，
  68 px 高要 289 px 宽（超屏），等比缩到放得下只剩 55 px 高。

### 局部刷新窗口：137 门 → **92 门（31%）**

这是本版最大的意外收获。**版面本身决定了窗口大小。**

- SSD1680 没有 window 命令，窗口靠 `0x01`（MUX = 门数−1）与 `0x0F`（SCN = 起始门）
  框出一段**连续栅极线**，映射是恒等式 `栅极 = 玻璃 x + 47`。
- 若时钟按串宽居中排版，`1` 进出分钟位会让整串变窄并**重新居中**，于是小时数字也跟着
  平移 —— 实测「枚举全部 1440 个 HH:MM、逐拍 diff 帧缓冲」的并集是**整个时钟**
  （玻璃 x 18..231 = 214 列），窗口省不下来。
- 改成**固定槽位**（4 个数字各占同宽格子，`1` 画窄但不缩小格子）后，并集缩到
  玻璃 x 140..231 → 窗口 **栅极 187..278（92 门）**，恰好等于并集，左右零余量。

### 三处真 bug（都不是推演出来的，是工具抓出来的）

| bug | 后果 | 怎么发现的 |
|---|---|---|
| `xr = x1 - t + 1`（数字右竖条） | 右边缘落在 `x+w`，墨迹跑出窗口推导所依据的格子，窗口会漏 1 门留下残影 | 把列扫描从「只扫格子宽」改成**全宽扫描**才现形 —— 原写法直接把越界墨迹丢掉 |
| 行 1 缓冲 `uint16_t r1[24]` | 最宽串（`2026年10月10日 周六 -40℃ 9999mV`，26 字符 + NUL）**越界写 3 个 `uint16_t` 进调用者栈帧** | 穷举公历全范围 × 温度/电压量程端点求最宽串 |
| 节气表止于 2050 | 2050-12-23 之后 **9 天找不到下一个节气**（`next_term()` 要跨进 2051） | 逐日走查边界的断言 |

### 符文（8×13，`BLE_RUNE_BITS`）

- 形状按用户手绘草图重测：**左侧是开口箭头**（上下各一臂交于中柱），不是「上臂共线于
  下弦」。原画法把左臂全放在中线上方，导致左下象限空白、字形偏斜。
- 预览图还暴露了一个**镜像 bug**：`tools/epd_face_model.py` 的 `uf_blit` 硬编码读字体
  数组，而固件传的是另一个数组 `BLE_RUNE_BITS`，于是镜像读到字体的空格字形，画了个空。
  修复后 `uf_blit` 增加 `data` 参数。
- 新增断言：符文**关于中心行镜像对称**（参考面板与草图都是如此），并附否定性自检
  —— 把初版的 16 字节位图喂回去，断言它会 FAIL（列 0..3 中线下方无墨）。

### 局部刷新期间的值必须冻结

栅极窗口是**竖直带**，带内**所有行**都被重写。行 1 的 `...mV` 起于玻璃 x=188，正落在
带内（140..231），所以电压/温度读数会被每分钟重绘 —— 末位漂移会被重画，`9→10` 的宽度
变化还会留下错位残影。`app.c` 因此新增快照：

```c
RAM uint16_t shown_mv   = 0xFFFF;   /* the value currently ON the glass */
RAM int16_t  shown_temp = 0;
if (paint) {
    if (full) { shown_mv = battery_mv; shown_temp = temperature; }
    epd_display(get_time(), shown_mv, shown_temp, full);
}
```

行 3 右端的设备名也在带内，但它由 `mac_public[]` 推出、每次开机的值恒定，重画无变化，
属于「靠恒定而安全」而非「靠冻结而安全」。诊断计数器则**刻意放在带外**（`H99T9B9L9`
宽 72 px、止于 x=74，带宽起点 140）—— 若能被局部刷新够到，就会把过期的计数重画到屏上。

### Flash / SRAM

| | v13.0 | v14.0 |
|---|---|---|
| 镜像 | 91260 B | **82460 B（−8800 B / −8.6 KB）** |
| `_end_bss_` | 0x84efb1 | **0x84efa1（余 4191 B）** |
| 局部窗口 | 137 门（46%）| **92 门（31%）** |

旧的 GFX 字体（`Dialog_plain_16` / `Special_Elite_Regular_30` /
`DSEG14_Classic_Mini_Regular_40`）不再被任何代码引用，由 `gc-sections` 整体裁掉 ——
镜像里已搜不到 `DSEG14`，也没有它们的符号。历法、排版、7 段几何全部在 Flash 执行，
`epd_display()` 只做绘制调用，所以 SRAM 反而比 v13.0 低 16 B。

三个承载这些字体的上游头文件（`src/font16.h` / `src/font30.h` / `src/font_60.h`，
共 82 KB）**同时删除**：它们只因旧 `epd.c` 而存在，现在没有任何文件 `#include` 它们。
留着是主动的隐患 —— 下一个人会去改一个根本不参与编译的字体。删除后重建，
镜像**逐字节不变**（已核）。需要时从上游 `atc1441/ATC_TLSR_Paper` 取回即可。

### 验证（三层，全部闭合）

1. **手册**：`EPD_WIN_GATES` 断言落在 SSD1680 的合法区间（MUX 16..296 / SCN 0..295）。
2. **离线**：`tools/verify_v14_layout.py` **66 项断言全 PASS** —— 格子边界、窗口 = 实际
   变化列的精确并集、行 1 最宽串、农历 9131 天逐日走查（日进一 / 月长 29|30 / 闰月一致 /
   两条独立路径互证 / 月均 29.5318 d 对 29.530589）、`epd_layout.h` 的编译期断言，
   以及**否定性自检**：把每个已修 bug 重新植入，确认对应断言真的会 FAIL
   （off-by-one、24 项缓冲、窗口少 1 门、多 1 门、退化笔画、行 3 多 1 字形、符文字形）。
3. **反汇编**：`EPD_BWR_213_Display` 里 `0x01` → `5B 00 01`（MUX=91 → 92 行）、
   `0x0F` → `BB 00`（SCN=187），推导出的窗口原封不动进了镜像；归档文件里能搜到新的
   16 字节符文、搜不到旧的。

**发布文件**：`firmware_releases/atc1441_clockonly_v14.0_2026-09-12_82460B.bin`
**SHA256**：`5528c0f2641db99c2828f27702dff9d7fe1e9f338c7dc5ccf81d26c6da77bf96`
**大小**：82460 字节（= 构建产物 `ATC_Paper.bin` 去掉末尾追加的 4 字节 CRC32；
CRC 本身 = `e5b09468`，可用 `zlib.crc32` 复核）
**SRAM**：`_end_bss_ = 0x84efa1`

### 工具链变化

- 新增 `tools/epd_face_model.py`：`epd_font.c` / `epd.c` / `calendar.c` 的**逐行镜像**，
  解析 `epd_layout.h` 的宏（含函数式宏 `CLOCK_SLOT_X`）与生成的头文件，术语与 C 的整数
  语义对齐（`/` 向零截断、`%` 取被除数符号）。预览与校验跑同一个模块，**不可能各自漂移**。
- 新增 `tools/gen_v14_tables.py`：生成 `font_unifont.h` / `font_chars.h` /
  `calendar_data.h`，并把编译进去的符文位图打进头文件注释。中文字形取自 GNU
  Unifont（≥13.0.04 起双授权，采用 **SIL OFL 1.1**，允许嵌入而不传染）。
- `tools/render_screen_preview.py` 重写：零第三方依赖（自带 PNG 编解码），
  参数 `--window` / `--compare <照片>` / `--debug H,T,B,L` / `--zoom` / `--tag`。
- **新增 `tools/archive/`**，退役 5 个校验脚本（`verify_gate_window.py` /
  `verify_partial_window.py` / `verify_ble_icon.py` / `verify_version_badge.py` /
  `verify_refresh_debug.py`）并逐条记录退役原因。它们不是坏了 —— 恰恰相反：
  在 v14.0 下它们**照样打印 PASS，却在断言固件早已不写的窗口**（自带硬编码 101/237、
  `CLOCK_X=50`）。**一个永远通过的检查比一个失败的检查更危险。**
- 文档配图改为入库且可重生成：`docs/images/screen-v14.0.png`（`render_screen_preview.py`）、
  `docs/images/reference-vs-v14.0.png`（`--compare`）、`docs/images/reference-panel.png`
  （参考面板原图，版面推导的唯一依据）。

**待办**：真机烧录确认（分钟局部刷 / 整点全刷、耗电、符文出现与消失时无残影）。

---

## v14.1 — 2026-09-12（右下角：广播名 / 版本号每 5 分钟交替；名字照参考图缩成 4 位）

**需求**：v14.0 把屏上的版本号去掉了（行 3 右下角让给了设备广播名）。用户同意两者
**交替显示**，cadence 定为 **5 分钟**。参考用图是官方 Bluetooth 图标
（用户从网上找来的），用于核对符文形状。

### 关键约束：切换必须发生在局部刷新带**内**

栅极窗口是竖直带（v14.0 = 玻璃 x 140..231），**带以外的像素在局部刷新时保持原样**。
v14.0 的广播名 `[A1B2C3]` 占玻璃 184..247，**跨出了带**（232..247 在带外）—— 只显示
名字时无所谓，但要在同一块地方交替两个字符串，跨出带外就意味着每次切换都会把旧串的
尾巴留在玻璃上，直到整点全刷才消失。

两个解法：加宽窗口（92 → 108 门），或把右下角收进带内。选了后者，理由：

- **零功耗代价**：加宽窗口是让**每一次**分钟局部刷新都多驱动 16 条栅极；收进带内则是
  一分钱不花。
- 右对齐到玻璃 x 231（= 带的最后一列）后，行 3 与行 1 的右边缘对齐了（行 1 最宽止于 228）。
- 名字照参考图缩成 **4 位十六进制** `[B2A1]`（广播名 `ESL_A1B2C3` 的末 4 位）——参考面板
  显示的 `[881A]` 正是这个形式，4 位 = 16 bit，区分设备绰绰有余。这其实是对参考图的
  **更贴近**，v14.0 的 6 位是偏离。

### 改动

| 项 | v14.0 | v14.1 |
|---|---|---|
| `ROW3_RIGHT_X` | 248（玻璃右缘内 1 px） | **231**（= 带的最后一列） |
| 广播名 | `[%02X%02X%02X]` 6 位 = 64 px | **`[%02X%02X]` 4 位 = 48 px** |
| 版本号 | 不显示 | **`FW_VERSION_STRING`，5 字符 = 40 px** |
| 交替 | — | 每 `ROW3_ALT_SECS`（300 s），`(t / 300) & 1` |
| 符文槽 | 跟随文字左缘（173..180） | **固定 `ROW3_RUNE_X`（172..179）** |
| 镜像 | 82460 B | 82468 B（+8，交替分支） |
| `_end_bss_` | 0x84efa1 | **0x84efa1（不变）** |
| 局部窗口 | 92 门（31%）| **92 门（不变）** |

两点说明：

- **符号槽必须固定**。两个串宽度不同（48 vs 40 px），若让符文跟着文字左缘走，它会每
  5 分钟横跳 8 px。改成常量 `ROW3_RUNE_X` 后符号纹丝不动，较窄的版本号只是多留 8 px 空隙。
- **交替的功耗是零**。切换骑在本来就发生的分钟局部刷新上，5 分钟或 1 分钟都一样 ——
  唯一的实际代价是该区域的像素每 5 分钟翻转一次，而每小时的整点全刷会清理残影。

### 符文对照（官方 Bluetooth 图标）

把官方图标的墨迹骨架抽出来对过：**竖干 + 右侧上下两个折角 + 左侧一个开口箭头
（上下各一臂交于中柱）**——与 v14.0 的 8×13 位图拓扑完全一致，镜像对称也一致。
官方原图 154×260（笔画粗约高度的 1/60），直接降采样到 8×13 会糊成一团；
8×13 上唯一可行的画法就是 1 px 笔画 + 45° 斜线，这正是 v14.0 的画法。**形状不改。**

### 验证

- `tools/verify_v14_layout.py` **75 项断言全 PASS**。新增 9 项：
  两个串各自「右对齐到 `ROW3_RIGHT_X`」「在玻璃内」「**在带内**」「避开固定符文槽」，
  加上「交替真的发生」「之间的分钟保持不变」「符文宽度与 `epd_layout.h` 预留一致」。
  窗口检查改为把**切换可改变的列**（184..231）折进并集 —— 结果仍是 140..231，
  所以窗口维持 92 门、左右零余量。
- `epd_layout.h` 新增编译期断言：右下角超出带、符文槽顶到日历文本、`ROW3_TEXT_MAX_ADV`
  与 6×8 不符，任一发生即 `#error`；`epd_font.h` 的 `EPD_RUNE_W` 改为**取自**
  `ROW3_RUNE_W`（单一真源，`epd_font.c` 仍与生成位图比对）。
- 反汇编：`0x01 → 5B 00 01`、`0x0F → BB 00` —— 窗口字节与 v14.0 逐字节相同。
- 镜像：`[%02X%02X%02X]` **搜不到了**，`[%02X%02X]` 与 `v14.1` 都在。

**发布文件**：`firmware_releases/atc1441_clockonly_v14.1_2026-09-12_82468B.bin`
**SHA256**：`512e1241941bab7249d91aa5f1b95182aa12b78ce83eb7b8612a723623287e3a`
**大小**：82468 字节（= `ATC_Paper.bin` 去掉末尾 4 字节 CRC32；CRC = `6d31ebab`）
**SRAM**：`_end_bss_ = 0x84efa1`

**待办**：真机烧录确认（分钟局部刷 / 整点全刷、耗电、**每 5 分钟的交替是否干净无残影**）。

---

## v14.2 — 2026-09-12（大时钟换回 DSEG14 数码管字体；修温度恒显 85 ℃）

两个变化，都来自用户看过 v14.1 实机后的反馈。

### 1. 温度恒显 85 ℃ —— 真回归，读错了传感器

**根因**：v13.0 屏上显示的是 `EPD_read_temp()`（SSD1680 内置温度传感器，真实 °C，
所以当时显示 `25'C`）。v14.0 重写版面时把它换成了 `app.c` 的 `temperature` 变量 ——
而那个变量来自 `get_temperature_c()`，一个**上游遗留函数：返回的是 TLSR8359 ADC 的
原始采样值，不是摄氏度**（真正的换算式 `579 - ((raw * 840) >> 13)` 在上游就被注释掉了，
函数名却叫 `_c`）。原始值几百上千，被 `ROW1_TEMP_MAX 85` 钳位 ⇒ **一天到晚显示 85**。

**修复**：显示源改回 `EPD_read_temp()`，与 v13.0 及 BLE 上报 (`ble_send_temp`) 同源。
附带两处：
- **(int8_t) 符号扩展** —— SSD1680 寄存器是二进制补码，0 ℃ 以下按无符号读会变成 200+；
  死区比较同样补上，否则真实温度跨 0 ℃ 时会无符号跳变 0↔255 触发无谓全刷。
- **删除 `get_temperature_c()` / `adc_temp_init()`** —— 修完后彻底无人使用（上游死代码，
  v13.0 时就已不参与显示），连同 `app.c` 的 `temperature` 全局一起删掉。

### 2. 大时钟换回 DSEG14 数码管字体（用户看过手绘实机后不要）

v14.0/v14.1 用手绘 7 段去追参考照片的 68 px 高度。用户看了实机表示更喜欢 v13.0 的
数码管效果 ⇒ 恢复 `DSEG14_Classic_Mini_Regular_40`（该字体文件在 git 历史里）。

- **尺寸**：位图字体只能整数倍放大。2 倍 = 290 px（超宽），**3/2 倍 = 60 px** ✓。
  3/2 最近邻展开：源像素按 2,1,2,1 的节奏变成 2×2 / 1×1 块，笔画宽度误差 ≤1 px，
  且无需逐像素除法。字体 40 px → 玻璃 60 px，居中于行 2（CLOCK_Y = 31）。
- **字体裁剪**：上游头文件 93 字形 / 8.2 KB；时钟只用 11 个（0-9 与冒号）。
  `gen_v14_tables.py` 新增 `emit_dseg()`：从 vendored 的 `tools/dseg14_upstream.h`
  重排出 **1.2 KB** 的 `src/font_dseg.h`（生成物，勿手改），并导出字体内在度量
  （advance 34 / 冒号 9 / 墨迹 x 4..29）供 `epd_layout.h` 推导版面。DSEG 字体为
  keshikan 作品，SIL OFL 1.1（见 `LICENSES/OFL-1.1.txt`）。
- **固定槽位保持**：DSEG14 十个数字的 advance 相同（34 px，`font_dseg.h` 内有断言），
  所以每个数字仍占固定槽位 —— 局部窗口的前提不变。新窗口 = 分钟两位墨迹
  （玻璃 137..227）∪ 右下角交替区（184..231）= **137..231 → 栅极 184..278（95 门，
  32%）**。反汇编核对：`0x01 → 5E 00 01`、`0x0F → B8 00`。
- **镜像**：82468 → **82864 B**（+396：+1.2 KB 字体，-0.8 KB 删掉的手绘栅格化器）。
  `_end_bss_ = 0x84efa1` 不变。

### 验证

- `tools/verify_v14_layout.py` **75 项断言全 PASS**。时钟段改为对 DSEG 位图建模：
  每字形墨迹仍在自己的槽位内、五槽不重叠、最宽 HH:MM 在玻璃内。新增源码断言：
  时钟走 DSEG 位图（`fill_quad` 已不在）、3/2 比例是命名常量、raw-ADC 温度函数已删。
- 否定性自检更新：字形被右移出槽位 ⇒ 「每字形墨迹不出槽位」断言 FAIL；
  字体头高报 `DSEG_INK_X0` ⇒ 窗口收窄、覆盖断言 FAIL。
- 反汇编：`0x01 → 5E 00 01`（MUX=94 → 95 门）、`0x0F → B8 00`（SCN=184）。
- 镜像：DSEG 位图在；`get_temperature_c` 不在。

**发布文件**：`firmware_releases/atc1441_clockonly_v14.2_2026-09-12_82864B.bin`
**SHA256**：`48213e2880214968c6cea4c5b96886435e4a55d44ea54b3dee910e70de67ec61`
**大小**：82864 字节（= `ATC_Paper.bin` 去掉末尾 4 字节 CRC32；CRC = `0afd5313`）
**SRAM**：`_end_bss_ = 0x84efa1`

**待办**：真机烧录确认（温度应显示真实室温；数码管时钟 60 px；每 5 分钟的交替）。
